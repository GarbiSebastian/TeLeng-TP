import random


random.randrange(100, 1000, 2)
# vectorsize = (10,0.01,0.01)
# x = box(size=vectorsize,color=color.red)
# y = box(size=vectorsize,color=color.green)
# z = box(size=vectorsize,color=color.blue)

# x.axis=vector(1,0,0)
# y.axis=vector(0,1,0)
# z.axis=vector(0,0,1)


# # floor = box (pos=(0,0,0), length=4, height=0.5, width=4, color=color.blue)
# # ball = sphere (pos=(0,4,0), radius=1, color=color.red)
# # ball.velocity = vector(0,-1,0)
# # dt = 0.01

# # while 1:
# #     rate (100)
# #     ball.pos = ball.pos + ball.velocity*dt
# #     if ball.y < ball.radius:
# #         ball.velocity.y = abs(ball.velocity.y)
# #     else:
# #         ball.velocity.y = ball.velocity.y - 9.8*dt

# #caja2 = box(pos=(1.1,0,0))

# #box(pos=[1,1,1],size=[1,2,3],axis=[1,0,0],up=[0,1,0],color=color.white)
# ellipsoid(pos=[1,1,1],size=[1,2,3],axis=[1,0,0],up=[0,1,0],color=color.white)
